{
  "name": "aicapsule",
  "version": "1.0.0",
  "private": true,
  "description": "AI Capsule - deployed as a single Render web service. Build compiles the React frontend; start runs the Express backend, which serves that build.",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
